apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # URL do Prometheus usando a rede interna do Docker (porta 9090)
    url: http://prometheus:9090
    isDefault: true
    editable: false